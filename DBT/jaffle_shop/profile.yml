your_profile_name:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost  # PostgreSQL host (can be an IP address or domain)
      user: ousheshharadhun  # PostgreSQL username
      password: alphabetagamma  # PostgreSQL password
      port: 5432  # PostgreSQL port (default is 5432)
      dbname: tutorial_db  # Name of the PostgreSQL database
      schema: public  # PostgreSQL schema (default is 'public')
